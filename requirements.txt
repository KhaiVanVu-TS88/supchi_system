# ===== REQUIREMENTS.TXT =====
# Danh sách thư viện Python cần thiết cho ứng dụng
# Cài đặt bằng: pip install -r requirements.txt

# --- Giao diện web ---
streamlit==1.35.0          # Framework tạo web app bằng Python

# --- Tải YouTube ---
yt-dlp==2024.5.27          # Tải video/audio từ YouTube (tốt hơn youtube-dl)

# --- Nhận dạng giọng nói ---
faster-whisper==1.0.3      # Speech-to-Text nhanh, tối ưu cho CPU
# CTranslate2 được cài tự động theo faster-whisper

# --- Xử lý âm thanh ---
ffmpeg-python==0.2.0       # Python wrapper cho FFmpeg

# --- Pinyin ---
pypinyin==0.51.0           # Chuyển chữ Hán sang Pinyin

# --- Dịch thuật ---
deep-translator==1.11.4    # Dịch ngôn ngữ (dùng Google Translate API miễn phí)

# --- Các thư viện hỗ trợ (thường đã có sẵn) ---
requests>=2.28.0           # HTTP requests (dùng cho dịch thuật)