# ===== BACKEND REQUIREMENTS =====
# Cài đặt: pip install -r requirements.txt

# --- API Server ---
fastapi==0.111.0           # REST API framework
uvicorn==0.30.1            # ASGI server để chạy FastAPI

# --- Tải YouTube ---
yt-dlp==2024.5.27          # Tải audio từ YouTube (luôn upgrade khi build)

# --- Nhận dạng giọng nói ---
faster-whisper==1.0.3      # Whisper tối ưu CPU (CTranslate2)

# --- Xử lý âm thanh ---
ffmpeg-python==0.2.0       # Python wrapper cho FFmpeg

# --- Pinyin ---
pypinyin==0.51.0           # Chuyển chữ Hán → Pinyin

# --- Dịch thuật ---
deep-translator==1.11.4    # Google Translate miễn phí

# --- Hỗ trợ ---
requests>=2.28.0
python-multipart==0.0.9    # Cần cho FastAPI form data